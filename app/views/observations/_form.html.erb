<%= form_for :observation , url: observations_path do |f| %>
  <%= f.label :content  %>
  <%= f.text_field :content  %><br>
  <!-- Must specify @commentable_type from proposal and experiments -->
  <%= f.hidden_field :experiment_id, value: experiment.id %>
  <%= f.submit "Submit" %><br>
<% end %>